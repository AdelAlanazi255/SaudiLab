---
id: lesson9
title: "Lesson 9: Form Inputs"
---

import LessonShell from '@site/src/components/LessonShell';

# Lesson 9: Form Inputs

Form inputs let users type and choose information. In this lesson, you will learn common input types and useful attributes.

<LessonShell
  course="html"
  current={9}
  total={10}
  lessonId="lesson9"
>

## What You'll Learn
- Common input types: <code>text</code>, <code>email</code>, <code>password</code>, <code>number</code>, <code>radio</code>, <code>checkbox</code>
- Key attributes: <code>placeholder</code>, <code>required</code>, <code>min</code>, <code>max</code>, <code>name</code>, <code>value</code>
- How to build a simple editable form

---

## Common Input Types

Use <code>text</code> for names, <code>email</code> for email addresses, and <code>password</code> for passwords.

Use <code>number</code> when users enter numbers, and <code>radio</code>/<code>checkbox</code> when users choose options.

---

## Helpful Attributes

<code>placeholder</code> shows a hint inside the input.

<code>required</code> makes a field mandatory.

For numeric inputs, <code>min</code> and <code>max</code> set allowed limits.

<code>name</code> and <code>value</code> help identify selected values in forms.

---

## Example

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Form Inputs</title>
  </head>
  <body>
    <h1>Quick Form</h1>

    <form onsubmit="return false;">
      <label for="name">Name</label>
      <input id="name" type="text" placeholder="Enter your name" />

      <button type="submit">Submit</button>
    </form>
  </body>
</html>
```

## Explanation

<div className="explanationBlock">
  <ul className="explanationText">
    <li><code>text</code>, <code>email</code>, <code>password</code>, <code>number</code> are common input types</li>
    <li><code>radio</code> and <code>checkbox</code> are used for selections</li>
    <li><code>placeholder</code> shows helper text and <code>required</code> enforces input</li>
    <li><code>min</code>/<code>max</code> limit number ranges, and <code>name</code>/<code>value</code> identify submitted values</li>
  </ul>
</div>

## Ready to Practice

Open the Try page and test different input types and attributes with RUN.

</LessonShell>

---
